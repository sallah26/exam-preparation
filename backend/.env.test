# Test Database Configuration
DATABASE_URL="mysql://sallah:@Ethiopia909@localhost:3306/addis_db"
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=sallah
DB_PASSWORD=@Ethiopia909
DB_NAME=addis_db

# Test Application Configuration
PORT=3001
NODE_ENV=test

# JWT Configuration for tests
JWT_SECRET="test-jwt-secret-key"
JWT_EXPIRES_IN="1h"
JWT_ISSUER="addis-admin-test"
JWT_AUDIENCE="addis-admin-test-users"
